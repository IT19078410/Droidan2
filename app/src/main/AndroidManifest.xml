<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.droidan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminFeedBack"></activity>
        <activity android:name=".AdminOwner" />
        <activity android:name=".AdminCustomer" />
        <activity android:name=".BookingApplication" />
        <activity android:name=".InsertImage" />
        <activity android:name=".FeedBack" />
        <activity android:name=".HomePage" />
        <activity android:name=".OwnerRegistration" />
        <activity android:name=".Login" />
        <activity android:name=".ImageAdapter" />
        <activity android:name=".CustomerRegistration" />
        <activity android:name=".Mainpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>